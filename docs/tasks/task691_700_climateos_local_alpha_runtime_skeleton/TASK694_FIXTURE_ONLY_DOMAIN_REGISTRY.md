# Task694 Fixture-Only Domain Registry

The registry exposes fixed demonstration entries for climate, water, land,
energy, carbon, and biodiversity. Every entry is labelled `fixture-only` and
names a human review authority.

The registry performs no plugin discovery, model loading, network call,
provider connection, or private extension. It demonstrates a future contract,
not scientific capability.
